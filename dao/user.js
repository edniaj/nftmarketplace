class userDAO {
    async createUser(username,password, walletAddress){
        /* example of DAO code 
        const [id] = db('user')...
        ... insert({
            ...
        }).returning(...) return this to the service layer
        */
    }
}